$ORIGIN .
$TTL 1800
<%= scope.lookupvar('::openshift_origin::hosts_domain') %> IN SOA ns1.<%= scope.lookupvar('::openshift_origin::hosts_domain') %>. hostmaster.<%= scope.lookupvar('::openshift_origin::hosts_domain') %>. (
                         2011112904 ; serial
                         60         ; refresh (1 minute)
                         15         ; retry (15 seconds)
                         1800       ; expire (30 minutes)
                         10         ; minimum (10 seconds)
                          )
                     NS ns1.<%= scope.lookupvar('::openshift_origin::hosts_domain') %>.
$ORIGIN <%= scope.lookupvar('::openshift_origin::hosts_domain') %>.
ns1	              A        127.0.0.1

